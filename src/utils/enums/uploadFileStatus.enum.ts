export enum UploadFileStatusEnum {
    completed = 'Completed',
    initiated = 'Initiated',
    inprogress = 'In Progress',
    failed = 'Failed',
    canceled = 'Canceled',
    queue = 'In Queue',
}
